import { HoverSelectedAreaDirective } from './hover-selected-area.directive';

describe('HoverSelectedAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverSelectedAreaDirective();
    expect(directive).toBeTruthy();
  });
});
