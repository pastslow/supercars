import { UpdateLineSizeDirective } from './update-line-size.directive';

describe('UpdateLineSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateLineSizeDirective();
    expect(directive).toBeTruthy();
  });
});
